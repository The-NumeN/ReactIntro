import { Button } from '../../components/button';

const User = () => {
    return (
        <div>
            <p>User</p>
            <Button label="Clique"></Button>
            <Button label="Clique ici"></Button>
        </div>
      );
};
export default User;